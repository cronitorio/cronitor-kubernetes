apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cronitor-k8s-agent.fullname" . }}-test-connection"
  labels:
{{ include "cronitor-k8s-agent.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cronitor-k8s-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
